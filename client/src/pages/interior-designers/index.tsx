import PageTemplate from "./page-template";
import { interiorDesignerPages } from "./config";

export const DigitalMarketingInteriorDesignersKL = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'digital-marketing-interior-designers-kuala-lumpur')!;
  return <PageTemplate config={config} />;
};

export const SEOForInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'seo-for-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const LocalSEOInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'local-seo-google-business-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const WebDesignForInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'web-design-for-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const PaidAdsForInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'paid-ads-for-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const ContentMarketingInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'content-marketing-for-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const SocialMediaMarketingInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'social-media-marketing-for-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const LeadGenerationInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'lead-generation-funnels-for-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const InteriorDesignerMarketingKL = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'interior-designer-marketing-kuala-lumpur')!;
  return <PageTemplate config={config} />;
};

export const InteriorDesignerMarketingMalaysia = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'interior-designer-marketing-malaysia')!;
  return <PageTemplate config={config} />;
};

export const InteriorDesignerMarketingSelangor = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'interior-designer-marketing-selangor')!;
  return <PageTemplate config={config} />;
};

export const InteriorDesignerMarketingPJ = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'interior-designer-marketing-petaling-jaya')!;
  return <PageTemplate config={config} />;
};

export const InteriorDesignerMarketingPenang = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'interior-designer-marketing-penang')!;
  return <PageTemplate config={config} />;
};

export const InteriorDesignerMarketingJB = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'interior-designer-marketing-johor-bahru')!;
  return <PageTemplate config={config} />;
};

export const MarketingCondoInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'marketing-condo-apartment-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const MarketingLandedInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'marketing-landed-house-interior-designers')!;
  return <PageTemplate config={config} />;
};

export const MarketingCommercialInteriorDesigners = () => {
  const config = interiorDesignerPages.find(p => p.slug === 'marketing-commercial-interior-designers')!;
  return <PageTemplate config={config} />;
};
